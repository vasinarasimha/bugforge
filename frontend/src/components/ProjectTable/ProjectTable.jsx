const formatDate = (date) => new Date(date).toLocaleDateString()
export default function ProjectTable({ projects = [], onEdit, onDelete, canEdit = true, canDelete = true }) {
  return <div className="table-responsive"><table className="table table-striped align-middle project-table"><thead><tr><th>Project Name</th><th>Description</th><th>Created Date</th><th>Created By</th><th>Actions</th></tr></thead><tbody>{projects.length ? projects.map((project) => <tr key={project.id}><td><strong>{project.project_name}</strong></td><td className="table-description">{project.description}</td><td>{formatDate(project.created_at)}</td><td>{project.created_by_name}</td><td><div className="d-flex gap-1"><button className="btn btn-outline-primary btn-sm" onClick={() => onEdit?.(project)}>View <i className="bi bi-eye" /></button>{canEdit && onEdit && <button className="btn btn-light border btn-sm" onClick={() => onEdit(project)} title="Edit"><i className="bi bi-pencil" /></button>}{canDelete && onDelete && <button className="btn btn-light border btn-sm text-danger" onClick={() => onDelete(project)} title="Delete"><i className="bi bi-trash" /></button>}</div></td></tr>) : <tr><td colSpan="5" className="text-center text-muted py-4">No projects found.</td></tr>}</tbody></table></div>
}
